# propertise of sets
# 1. sets sre unordered (Elements order desnt matter)
# 2. Sets are unindexed (Cannot access elements by index)
# 3. There is no way to change items in sets
# 4 Sets cannot contain duplicate values.


#=======  methods of sets  ===============

s={54,33,46,34,44,33,"moni",44,44,44}

# print(s,type(s)) # this way also run the code executes.


# s.add("add_method") # to add a value of sets

# ss=s.__len__() # length function
# print(ss, type(s))

s.remove(54) #remove the Value given by the input

s.clear # it clear the value ( all Value)

print(s)